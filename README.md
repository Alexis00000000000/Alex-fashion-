# Alex-fashion-
let cart = JSON.parse(localStorage.getItem('cart')) || []; const cartBtn = document.getElementById('cart-btn'); const addToCartBtn = document.getElementById('add-to-cart'); const cartModal = document.getElementById('cart-modal
